"use client";
import { useState } from "react";
import { Check, Eye, EyeOff, KeyRound, Save, Store } from "lucide-react";
import { TestMode } from "@/components/ui/test-mode";
export function SettingsForm(){const [saved,setSaved]=useState(false);const [show,setShow]=useState(false);return <div className="grid gap-5 xl:grid-cols-[1fr_340px]"><div className="space-y-5"><section className="card p-5 sm:p-6"><div className="flex items-center gap-2"><Store size={17} className="text-brand"/><h2 className="text-sm font-bold">Merchant profile</h2></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Business name" value="Nova Electronics"/><Field label="Merchant ID" value="mer_nova_001" mono/><Field label="Currency" value="INR — Indian Rupee"/><Field label="Timezone" value="Asia/Calcutta"/></div></section><section className="card p-5 sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><KeyRound size={17} className="text-brand"/><h2 className="text-sm font-bold">Razorpay Test credentials</h2></div><TestMode compact/></div><p className="mt-3 text-xs leading-5 text-muted">Secrets are read server-side from environment variables and are never serialized to the browser.</p><div className="mt-5 space-y-4"><Field label="Key ID" value="rzp_test_••••••••4G9K" mono/><label className="block"><span className="mb-1.5 block text-[10px] font-semibold text-muted">Key secret</span><div className="flex items-center rounded-xl border border-line bg-[#FAFBFC] px-3"><input readOnly value={show?"server-side-secret-demo":"••••••••••••••••••••••"} className="mono flex-1 bg-transparent py-3 text-xs outline-none"/><button onClick={()=>setShow(!show)} className="text-muted">{show?<EyeOff size={15}/>:<Eye size={15}/>}</button></div></label><Field label="Webhook signing secret" value="••••••••••••••••••••••" mono/></div></section><button onClick={()=>{setSaved(true);setTimeout(()=>setSaved(false),1800)}} className="btn-primary"><Save size={15}/>{saved?"Settings saved":"Save changes"}</button></div><aside className="space-y-5"><div className="card p-5"><p className="eyebrow">Security posture</p><ul className="mt-4 space-y-3 text-xs text-muted">{["Test keys only","HMAC signature verification","Webhook idempotency","HttpOnly session cookie","Merchant isolation","Rate-limited agent API"].map(x=><li className="flex gap-2" key={x}><Check size={13} className="mt-0.5 text-mint"/>{x}</li>)}</ul></div><div className="rounded-2xl border border-[#FFD0D2] bg-rose-soft p-5"><p className="text-xs font-bold text-[#B52E33]">Live keys are rejected by policy</p><p className="mt-2 text-[11px] leading-5 text-[#8E5558]">This hackathon build is intentionally locked to Razorpay Test Mode. No real money should be processed.</p></div></aside></div>}
function Field({label,value,mono}:{label:string;value:string;mono?:boolean}){return <label className="block"><span className="mb-1.5 block text-[10px] font-semibold text-muted">{label}</span><input readOnly value={value} className={`${mono?"mono":""} w-full rounded-xl border border-line bg-[#FAFBFC] px-3 py-3 text-xs outline-none`}/></label>}
