import { Download, Search } from "lucide-react";
import { orders } from "@/lib/demo-data";
import { formatINR } from "@/lib/money";
import { SectionHeading } from "@/components/ui/section-heading";
import { Badge } from "@/components/ui/badge";
export default function Orders(){return <><SectionHeading eyebrow="Transaction Operations" title="Orders" description="An order becomes paid only after server-side Razorpay signature or verified webhook confirmation." action={<button className="btn-secondary"><Download size={15}/> Export</button>}/><div className="card overflow-hidden"><div className="flex items-center gap-2 border-b border-line p-4"><Search size={15} className="text-muted"/><input className="w-full bg-transparent text-xs outline-none" placeholder="Search by order, agent or customer…"/></div><div className="overflow-x-auto"><table className="w-full min-w-[820px] text-left"><thead><tr className="bg-[#FAFBFC] text-[10px] uppercase tracking-wider text-muted"><th className="px-5 py-3">Order</th><th className="px-4 py-3">Customer / agent</th><th className="px-4 py-3">Items</th><th className="px-4 py-3">Source</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Created</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-t border-line hover:bg-[#FBFBFD]"><td className="mono px-5 py-4 text-[11px] font-bold text-brand">{o.id}</td><td className="px-4 py-4 text-xs font-semibold">{o.customer}</td><td className="px-4 py-4 text-xs text-muted">{o.items}</td><td className="px-4 py-4 text-xs">{o.source}</td><td className="px-4 py-4 text-xs font-bold">{formatINR(o.amount)}</td><td className="px-4 py-4"><Badge tone={o.status==="PAID"?"green":"red"}>{o.status}</Badge></td><td className="px-4 py-4 text-[10px] text-muted">{o.date}</td></tr>)}</tbody></table></div></div></>}
